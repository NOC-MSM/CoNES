Bootstrap: library
From: ubuntu:18.04

%post
    
    echo $SSH_AUTH_SOCK


    apt-get install -y \
                       git 

mkdir -p ~/.ssh
ssh-keyscan  github.com >> ~/.ssh/known_hosts 
echo $PWD    
    git clone git@github.com:NOC-MSM/singularity-nemo.git
