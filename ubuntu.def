Bootstrap: library
From: ubuntu:18.04

%post
	
	adduser --disabled-password --gecos "" nemo

	mkdir /testnemo
	echo "Some test results" >> /testnemo/nemo_results.txt
	chown -R nemo:nemo /testnemo
	chmod -R 777 /testnemo

%labels
    Author c.wood@epcc.ed.ac.uk
    Version v0.0.1

%runscript
    NOW=`date` 
    echo "Hello World\n"
    echo $NOW

%help
    This is a demo container used to illustrate a def file that uses all
    supported sections.
