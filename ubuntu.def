Bootstrap: library
From: ubuntu:20.04

%labels
    Author c.wood@epcc.ed.ac.uk
    Version v0.0.1

%runscript
    echo $SSH_AUTH_SOCK

    NOW=`date` 
    echo "Hello World\n"
    echo $NOW

%post
    echo 'export my_env=10' >>$SINGULARITY_ENVIRONMENT
    echo 'export LD_LIBRARY_PATH=/opt/hdf5/install/lib' >>$SINGULARITY_ENVIRONMENT

%help
    This is a demo container used to illustrate a def file that uses all
    supported sections.
